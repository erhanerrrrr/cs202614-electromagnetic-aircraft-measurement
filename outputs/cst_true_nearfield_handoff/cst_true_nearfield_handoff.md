# CST True Near-Field Handoff

This handoff turns the true-monitor workpack into an operator action list.
It is generated by `python code\build_true_nearfield_handoff.py`.

## Immediate Decision

- Export the two required `full_grid_162` true near-field monitor CSV files first.
- Do not spend CST time on 32/120 reduced layouts before the full-grid monitor files exist; they can be derived from the 162-point CSV.
- After export, run `python code\run_true_nearfield_gate.py --required-only`.

## Required Full-Grid Exports

| Sample | Priority | Target CSV | Expected rows | Exists | Gate status |
|---|---|---|---:|---|---|
| L1_short_dipole_z_1p2G | required | `data\cst_exports\level1_true_nearfield\L1_short_dipole_z_1p2G_true_nearfield.csv` | 486 | False | pending_source |
| L1_halfwave_dipole_z_1p2G | required | `data\cst_exports\level1_true_nearfield\L1_halfwave_dipole_z_1p2G_true_nearfield.csv` | 486 | False | pending_source |

## CSV Contract

- One row per `sample_id, frequency_hz, sensor_id, polarization`.
- The required full-grid row count is `162 sensors * 3 components = 486` per case.
- Use Cartesian components `Ex`, `Ey`, and `Ez`; keep complex values as `e_real` and `e_imag`.
- Keep `extraction_method` as `CST true near-field monitor interpolated on spherical shell`.

Required columns:

`sample_id`, `frequency_hz`, `sensor_id`, `x_m`, `y_m`, `z_m`, `theta_deg`, `phi_deg`, `radius_m`, `polarization`, `e_real`, `e_imag`, `cst_project`, `cst_monitor_item`, `extraction_method`

## Algorithm Follow-Up Commands

| Order | Gate | Command | Purpose |
|---:|---|---|---|
| 0 | current_state | `CST export required before algorithm rerun.` | All queued true-monitor source files are still absent. |
| 1 | after_full_grid_exports | `python code\check_true_nearfield_dropzone.py --required-only --full-grid-only` | Preflight the required true-monitor CSV contract, row counts, components, and sensor subsets. |
| 2 | after_full_grid_exports | `python code\run_true_nearfield_gate.py --required-only` | Confirm required full-grid true-monitor files are no longer pending_source. |
| 3 | after_required_full_grid_passes_file_count | `python code\derive_true_nearfield_layout_exports.py --sample-id L1_short_dipole_z_1p2G` | Derive queued 32/120 layouts for the first required case from the full-grid monitor CSV. |
| 4 | after_required_full_grid_passes_file_count | `python code\derive_true_nearfield_layout_exports.py --sample-id L1_halfwave_dipole_z_1p2G` | Derive queued 32/120 layouts for the second required case from the full-grid monitor CSV. |
| 5 | after_derivation | `python code\run_true_nearfield_gate.py --required-only` | Compare full/reduced required layouts and decide reference_match versus needs_physical_rerun. |
| 6 | if_needs_physical_rerun | `python code\run_cst_source_model_sweep.py` | Rerun source-model support calibration on authoritative monitor data. |
| 7 | if_needs_physical_rerun | `python code\run_spherical_nf_ff_baseline.py` | Rerun the scalar SWE sanity baseline before any reduced-layout claim. |
| 8 | if_needs_physical_rerun | `python code\run_cst_huygens_baseline.py` | Rerun the Huygens diagnostic and keep it separate from report-safe proof until it passes. |

## Current Gate Summary

- Queue rows: 18
- Pending source rows: 18
- Status counts: {'pending_source': 18}

## Generated Files

| File | Purpose |
|---|---|
| `cst_operator_action_sheet.csv` | Full queue with action kind, target paths, file existence, and gate status. |
| `expected_true_monitor_files.csv` | Full-grid monitor CSVs that CST should produce first. |
| `post_export_algorithm_commands.csv` | Commands for the algorithm operator after export. |
| `handoff_summary.json` | Machine-readable counts and blockers. |
